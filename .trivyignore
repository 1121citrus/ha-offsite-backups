# CVEs with fix versions identified by Trivy but not yet available in the
# AL2023 package repositories. Remove entries once the fixed packages land.
#
# All entries previously here (glib2, libacl, and cpython findings from
# the 2026-07-25 rescan) are resolved as of aws-backup-base's 2026-09-03
# AL2023 digest refresh (sha256:181f98c...ce284), which this image
# inherits -- confirmed absent from a fresh-DB rescan against the
# refreshed base. The one entry below is new as of that same rescan.

# openssl-fips-provider-latest / openssl-libs (inherited from
# aws-backup-base): Denial of Service via unbounded memory growth in
# QUIC server — fix: 1:3.5.7-2.amzn2023.0.2 (not yet in repo)
CVE-2026-14456
